name: Hello Agile
# Testing dev trigger
on:
  push:
    branches: [ dev ]

  pull_request:
    branches: [ main ]

jobs:
  say_hello:
    runs-on: ubuntu-latest

    steps:
      - name: Print Hello
        run: echo "Hello from GitHub Actions!"
        